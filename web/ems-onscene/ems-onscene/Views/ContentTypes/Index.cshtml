@model IEnumerable<ems_onscene.Models.EntityModels.ContentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>Content Type</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.ContentType1
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContentType1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContentType1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContentType1 })
        </td>
    </tr>
}

</table>
